package dev.ikm.komet.layout;

import dev.ikm.komet.layout.area.KlArea;

/**
 * Consider an observable knowledge layout that can dynamically recompute.
 */
public interface KnowledgeLayout {
    KlArea rootArea();
}
